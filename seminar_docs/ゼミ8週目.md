# 学習方法

$$
L(\theta) = \frac{1}{B} \sum_{k=1}^{B} \left( y_k - Q(s_k, a_k; \theta) \right)^2
$$

$$
y_k = \begin{cases}
r_k & (\text{終端: } i = T-1) \\
r_k + \gamma \max_{a'} Q(s'_k, a'; \theta^{-}) & (\text{非終端})
\end{cases}
$$

ただし、
# モデルの評価方法

元論文の実装：step 7〜40 平均の abs(Bias)・RMSE・MAEが3つとも同時改善したものをbest modelとして保存。